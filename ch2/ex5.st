| input subtotal tipRate tip total output |input := (UIManager default request: 'Enter subtotal and tip rate:') parseLiterals.subtotal := (input at: 1).tipRate := (input at: 2).tip := tipRate / 100 * subtotal.total := tip + subtotal.output := WriteStream on: String new.output << 'The tip is '.output << (tip round: 2) asString.output << ' and the total is '.output << (total round: 2) asString.UIManager default alert: output contents.